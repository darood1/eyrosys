const slides=[...document.querySelectorAll('.slide')],dots=[...document.querySelectorAll('.dot')];let i=0,t;function show(n){if(!slides.length)return;i=(n+slides.length)%slides.length;slides.forEach((s,x)=>s.classList.toggle('active',x===i));dots.forEach((d,x)=>d.classList.toggle('active',x===i));clearInterval(t);t=setInterval(()=>show(i+1),6000)}document.querySelector('.next')?.addEventListener('click',()=>show(i+1));document.querySelector('.prev')?.addEventListener('click',()=>show(i-1));dots.forEach((d,x)=>d.addEventListener('click',()=>show(x)));document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.site-header nav').classList.toggle('open'));document.querySelectorAll('.flip-card').forEach(c=>c.addEventListener('click',e=>{if(matchMedia('(hover: none)').matches&&!c.classList.contains('flipped')){e.preventDefault();c.classList.add('flipped')}}));document.querySelectorAll('.web-form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();f.querySelector('.form-status').textContent='Form completed. Connect this static form to your preferred form-processing service or website backend to receive submissions.'}));show(0);